<li>
	{{burger_name}}

	<button class="devour-state btn btn-success" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}DEVOUR!{{else}}EAT ANOTHER!{{/if}}
	</button>

	<button class="delete-burger btn btn-danger" data-id="{{this.id}}">DELETE!</button>
</li>